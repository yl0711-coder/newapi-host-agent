# newapi-host-agent 配置示例。复制为 agent.env、chmod 600,真 token 不要提交。
MONITOR_URL=http://172.26.10.97:8090
INGEST_TOKEN=替换为 monitor 的 MONITOR_INGEST_TOKEN
# NODE_NAME 建议填该机的 Lightsail 实例名(便于和云侧指标对应);留空=hostname
# NODE_NAME=Ubuntu-NexusAPI-Slave-1
